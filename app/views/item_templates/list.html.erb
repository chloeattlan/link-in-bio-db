

<h1> Hello world! </h1>

<% @list_of_items.each do |item| %>
  <div class = "item">
  <imc src = "<% item.thumbnail_url %>" class = "thumbnail">
  <a href = "<%= item.link_url %>">
  <%=item.link_description%>
  </a>
  </div>
  <% end %>
